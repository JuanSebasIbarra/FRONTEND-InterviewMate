function LoginPage() {
  return (
    <section className="page-card">
      <h2>Login</h2>
      <p>Plantilla de ruta pública para inicio de sesión (sin lógica de autenticación).</p>
    </section>
  )
}

export default LoginPage
